# Hadoop Program Compile

**Status** » Finished
**Language** » C++ *-std=c++11*

This code is a command wrapper to perform the source code compile, packaging and cleanup on a Java Hadoop program.

